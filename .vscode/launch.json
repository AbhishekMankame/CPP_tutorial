{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SDL2 Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "MIMode": "gdb"
        }
    ]
}
